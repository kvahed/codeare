#include "Matrix.h"
//#include "ReconClient.h"

/*template<> void 
Matrix< complex<float> >::SetRaw () const {
	ReconClient rc;
	rc.SetRaw(_M, Size());
};


template<> void 
Matrix< short >::SetPixel        () const {
	ReconClient rc;
	rc.SetPixel(_M, Size());
	};*/

